apply plugin: 'jetty'
apply plugin: 'jettyEclipse'

jettyEclipse {
    contextPath = '/ui'
    httpPort = 8080
    stopPort = 8090
}

jettyEclipseRun.dependsOn war

jettyRun {
    httpPort = 8080
    stopPort = 8090
    stopKey = null
}
