package pl.info.rkluszczynski.image.core.ciratefi.data;

/**
 * Created by Rafal on 2014-04-24.
 */
public class I3DGRY {

/*
    IMGGRY* vet; BYTE backgv; int nsv,nlv,ncv;
    void aloca(int nsp, int nlp, int ncp);
    void desaloca();
    void copia(const I3DGRY& x);
    public:
    void fill(BYTE e1);
    void resize(int nsp, int nlp, int ncp);

    friend class I4DGRY;

    int ns() const { return nsv; }
    int nl() const { return nlv; }
    int nc() const { return ncv; }
    BYTE& backg() { return backgv; }

    I3DGRY(int nsp=0, int nlp=0, int ncp=0);   // (default) constructor
    I3DGRY(const I3DGRY& x);             // copy constructor
    I3DGRY& operator= (const I3DGRY& x); // copy assignment
    ~I3DGRY();                          // destructor

    IMGGRY& operator() (int i) {
        if (i<0 || nsv<=i) erro("Erro I3DGRY::operator() - Indice invalido");
        return vet[i];
    }
    BYTE& operator() (int s, int l, int c) {
        if (0<=s && s<nsv && 0<=l && l<nlv && 0<=c && c<ncv)
            return vet[s].lin[l][c];
        else return backgv;
    }
    void ImpTga(string nome);
*/

}
